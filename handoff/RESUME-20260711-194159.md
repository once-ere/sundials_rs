# RESUME — sundials_rs Phase 6 (arkode) — snapshot 2026-07-11 19:41:59

**Read this file first, then `STATE-20260711-194159.md` (same folder) for
pinned technical detail.  Do not re-explore the codebase from scratch —
CLAUDE.md + PROGRESS.md + `git log` + these two files are the complete
resume context.**

## What this project is

100% faithful line-for-line translation of LLNL SUNDIALS v7.7.0 (C) into
pure Rust at `/Users/nsh/Developer/code/rust/sundials/sundials_rs/`
(7-crate Cargo workspace).  Reference C tree: `../sundials-7.7.0/`
(NEVER modify).  Donor port: `crates/cvode_rs` (+ `../cvode_rs/`).
User is Nash: terminal novice, terse; "continue with X" means immediately
resume concrete port work on X, no questions.

Hard rules (full text in CLAUDE.md — binding):
- zero `unsafe`, zero external deps, zero warnings
- exact C API names / return flags; preserve FP arithmetic order
- one C file -> one Rust module (same base name)
- example output byte-identical via `sundials_utils::fmt_e/fmt_f/fmt_g`
  (never `{:e}`)
- commit per file/group; commit trailer:
  `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`
- excluded backends (GPU/MPI/KLU/SuperLU/LAPACK/Fortran/XBraid) stay out
- missing symbol => port it from `../sundials-7.7.0/src|include`, never invent

## Exact current state

- HEAD = `d67b488` "lsrkstep + all four ark_analytic_lsrk/ssprk examples
  LOCAL-C verified".  Working tree CLEAN.  Everything described below is
  committed.
- Gate status at snapshot: `cargo build --workspace` 0 warnings;
  `cargo clippy -p arkode_rs` 0 warnings; `cargo test --workspace`
  254 passed 0 failed; harness `tools/verify_examples.sh all` =>
  **65 IDENTICAL/LOCAL-C entries, 23 non-passing entries which are ALL
  known/pending items** (list below — no regressions).

### arkode modules DONE (all committed, all verified)

arkode_impl, arkode (core Evolve/Init/Hin/ewt-rwt/CompleteStep...),
arkode_io (large subset), arkode_interp (Hermite+Lagrange),
arkode_adapt, arkode_root, arkode_butcher (+erk/dirk tables),
arkode_ls + arkode_ls_impl (system half AND mass half),
arkode_arkstep{,_impl,_io,_nls} (Newton + fixed-point loops, all
identity/fixed/timedep-mass residual+FP variants, deduce_rhs,
autonomous/TPA), arkode_erkstep{,_impl,_io},
arkode_sprk + arkode_sprkstep{,_impl,_io} (incl. compensated sums),
arkode_lsrkstep{,_impl,_io} (RKC/RKL STS + SSPs2/s3/43/104 + DomEig
machinery).  Core additions: SUNDomEigEstimator refactor (see STATE),
sunCompensatedSum, N_VLinearCombination, SUNRpowerR fix.

### arkode examples verified (28 arkode harness entries pass)

IDENTICAL vs shipped refs: ark_analytic_nonlin, ark_analytic,
ark_brusselator, ark_brusselator_fp (+_fp_1), ark_robertson,
ark_robertson_root, ark_KrylovDemo_prec (+_1, +_2),
ark_kepler LEAPFROG variant.
LOCAL-C (byte-identical to a locally built 7.7.0 C binary; shipped
refs stale): 12 ark_kepler variants, ark_analytic_lsrk, _lsrk_varjac,
_lsrk_domeigest (+ its max_iters arg variant), ark_analytic_ssprk.
Local references live in `localref/arkode/C_serial/` (committed).

### The 23 currently non-passing harness rows (all expected)

- Pre-existing documented exceptions (do NOT touch): cvRoberts_dnsL,
  cvRoberts_dns_negsol, cvDiurnal_kry, cvDiurnal_kry_bp,
  cvDirectDemo_ls, cvKrylovDemo_ls, cvParticle_dns, cvPendulum_dns,
  kinLaplace_picard_kry, kinRoboKin_dns, idaFoodWeb_bnd,
  idasSlCrank_dns, idasSlCrank_FSA_dns.
- Pending arkode ports (the actual to-do list):
  - `ark_analytic_mels` — needs matrix-embedded SUNLinearSolver support
  - `ark_analytic_partitioned_{forcing,splitting}` (+3 splitting-method
    variants) — needs splittingstep + forcingstep modules
  - `ark_brusselator_mri`, `ark_brusselator_1D_mri` — needs
    arkode_mri_tables + arkode_mristep
  - `ark_robertson_constraints` — needs ARKodeSetConstraints path
    exercised (arkCheckConstraints is ported; example not yet written)
  - `ark_analytic_arkode.scalar_tolerances_..._table_names_...` arg
    variant — needs arkode_cli.c (ARKodeSetOptions) module

## Next work units (ledger order; pick per user instruction)

1. **splittingstep + forcingstep** (src/arkode/arkode_splittingstep*.c,
   arkode_forcingstep.c, arkode_splittingstep_coefficients.c) ->
   unlocks 5 ark_analytic_partitioned_* refs.
2. **mri tables + mristep** (arkode_mri_tables.c, arkode_mristep*.c —
   large) -> ark_brusselator_mri, _1D_mri, kepler mri cousins,
   imexmri variants.
3. **arkode_cli.c** (ARKodeSetOptions + stepper SetOptions hooks;
   sundials_cli core module already exists) -> remaining arg variants.
4. **ark_robertson_constraints example** (SetConstraints already in io?
   verify; mostly example work).
5. bandpre/bbdpre preconditioner modules (donor: cvode_bandpre/bbdpre).
6. relaxation module (arkode_relaxation.c; stubs exist).
7. arkode.c leftovers: Reset/Resize, arkPredict_* interpolatory
   predictors (cases 1–4 of arkStep_Predict fall to trivial today —
   predictor method 1 IS exercised+verified via ark_robertson).
8. Heat/1D PDE examples (ark_heat1D, ark_heat1D_adapt), ark_KrylovDemo
   remaining refs, ark_onewaycouple_mri..., per
   `ls ../sundials-7.7.0/examples/arkode/C_serial/*.out`.

## How to resume (mechanics)

1. `cd /Users/nsh/Developer/code/rust/sundials/sundials_rs`
2. Confirm clean state: `git log --oneline -3`, `git status`.
3. Re-run gate if unsure:
   `cargo build --workspace 2>&1 | tee /tmp/build.log`
   `cargo test --workspace`
   `tools/verify_examples.sh arkode_rs` then Read `logs/summary.txt`.
4. The local C reference build lives in a SESSION-SPECIFIC scratchpad
   that is probably GONE.  Rebuild recipe is in STATE file §"Local C
   build" — needed for any LOCAL-C comparison or new stale-ref case.
5. Follow the per-unit workflow in STATE §"Porting workflow that has
   worked 28-for-28".
